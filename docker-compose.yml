version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: nosara-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: nosara
      POSTGRES_PASSWORD: nosara
      POSTGRES_DB: nosara
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    container_name: nosara-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  miniodata:
